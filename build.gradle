plugins {
	id 'net.fabricmc.fabric-loom' version "1.17-SNAPSHOT"
	id 'maven-publish'
	id "me.modmuss50.mod-publish-plugin" version "0.7.4"
	id "com.modrinth.minotaur" version "2.+"
}
apply from: "main.gradle"

// Should exist on all loaders and is loader specific
repositories {
	mavenLocal()
	maven {url "https://modmaven.dev/" }// UnionLib
	maven {url "https://girafi.dk/maven/"}
	maven {url "https://www.cursemaven.com"} //CurseForge
    maven {url "https://repo.spongepowered.org/maven"} // mixin
    maven {url 'https://maven.parchmentmc.org'}//Parchment
}

dependencies {
	minecraft "com.mojang:minecraft:${project._minecraft_version}"
	implementation "net.fabricmc:fabric-loader:${project._loader_version}"
	implementation "net.fabricmc.fabric-api:fabric-api:${project._loader_api_version}"

	implementation ("com.stereowalker.unionlib:UnionLib:${_minecraft_version}-${_unionlib_version}-${_loader}")

	compileOnly ("mezz.jei:jei-${_minecraft_version}-common-api:${_jei_version}")
	compileOnly ("mezz.jei:jei-${_minecraft_version}-fabric-api:${_jei_version}")
	implementation ("mezz.jei:jei-${_minecraft_version}-fabric:${_jei_version}")
}
